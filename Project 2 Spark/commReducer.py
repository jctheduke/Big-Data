import sys
from operator import itemgetter

for line in sys.stdin:
    line = line.strip()  
    splitLine = line.split(",")
    #GT = eval(splitLine[1])
    #CS = eval(splitLine[2])
    #print '%d,%d' % (GT,CS)

